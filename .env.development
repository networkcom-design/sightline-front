# Desarrollo. Vite carga este archivo solo con `npm run dev`.
#
# Va versionado a proposito: no tiene ningun secreto y hace que el proyecto
# funcione recien clonado, sin que nadie tenga que adivinar el puerto.
VITE_API_URL=http://localhost:8080
